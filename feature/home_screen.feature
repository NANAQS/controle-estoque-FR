Feature: Home Screen Ativa (ADM)

  Scenario: Click Produtos (Drawer)
    Given quando cliente clicar nos "Produtos" no drawer
     When --
     Then enviar cliente para tela de Produtos

  Scenario: Click Categoria (Drawer)
    Given quando cliente clicar nos "Categoria" no drawer
     When --
     Then enviar cliente para tela de Categoria

  Scenario: Click Fornecedores (Drawer)
    Given quando cliente clicar nos "Fornecedores" no drawer
     When --
     Then enviar cliente para tela de Fornecedores

  Scenario: Click Mov. Entrada (Drawer)
    Given quando cliente clicar nos "Mov. Entrada" no drawer
     When --
     Then enviar cliente para tela de Mov. Entrada

  Scenario: Click Mov. Saída (Drawer)
    Given quando cliente clicar nos "Mov. Saída" no drawer
     When --
     Then enviar cliente para tela de Mov. Saída

  Scenario: Click Usuarios Plataforma (Drawer)
    Given quando cliente clicar nos "Usuarios Plataforma" no drawer
     When --
     Then enviar cliente para tela de Usuarios Plataforma

  Scenario: Click Logs de Atividade (Drawer)
    Given quando cliente clicar nos "Logs de Atividade" no drawer
     When --
     Then enviar cliente para tela de Logs de Atividade