const positionIcon = {
  padding: 10,
  width: 50,
};

const container = {
  display: "flex",
  justifyContent: "space-around",
  flexDirection: "row",
};

const wellcomeFont = {
  fontWeight: "bold",
  fontSize: 100,
};

const inputsContainer = {
  padding: 10,
  paddingTop: 5,
  width: 200,
};

const title = {
  fontSize: 30,
};

const inputs = {
  marginBottom: 4,
};

const buttonLogin = { textAlign: "center", width: "100%", height: 55 };

export default {
  positionIcon,
  container,
  wellcomeFont,
  inputsContainer,
  title,
  inputs,
  buttonLogin,
};
